On peut dire que le code est compilé une fois puis exécuté des milliers,voir des millions de fois(par les serveur pas exemple).un temps de compilation long ne coute cher qu'aux programmeur pendants qu'ils travaillent,mais un temps d'execution lent se repercute sur chaque utilisatuer à chaque lancement a grand potentielle .c'est pourquoi les optimisation de performance ciblent presque toujours l'execution plutot que la compilation.on peut donc direcque le cout de la compilation est fixe et localisé, celui de l'execution est recurrent et multiplié. Dans notre devoir on a ajoute 5 en-tete en plus sur un de nos programme soit donc 5 #incude(string.h,math.h,time.h,ctype.h et stddool.h) par exemple.